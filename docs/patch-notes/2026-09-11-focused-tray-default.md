# Chat-focused tray by default

- The system tray starts in chat-focused view on first use, then remembers your last selected view for future launches. Existing saved choices are preserved; switch views using the toggle beside pins.
